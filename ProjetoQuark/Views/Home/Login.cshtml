@model ProjetoQuark.Models.ModelLogin

@{
    ViewBag.Title = "Login";
    Layout = "";
}

@ViewBag.confCadastro
@ViewBag.msgLogar

@using (Html.BeginForm("Login", "Home", FormMethod.Post))
{
<fieldset>
    <br />
    <h2>Entrar</h2>
    <br />
    <div class="form-group">
        <div class="input-group">
            <span class="input-group-addon"><i style="font-size:20px;" class="fas fa-envelope"></i></span>

            @Html.EditorFor(x => x.emailCli, new
       {
           htmlAttributes = new { id = "txtUsuario", senha = "txtUsuario", type = "text", @class = "form-control", placeholder = "email@emai.com", required = "required", maxlength = "30" }
       })

            @Html.ValidationMessageFor(x => x.emailCli, "", new { @class = "text-danger" })

        </div>
    </div>

    <div class="form-group">
        <div class="input-group">
            <span class="input-group-addon"><i style="font-size:20px;" class="fas fa-lock"></i></span>

            @Html.EditorFor(x => x.senha, new
       {
           htmlAttributes = new { id = "txtSenha", senha = "txtSenha", type = "password", @class = "form-control", placeholder = "Digite sua senha", required = "required", maxlength = "10" }
       })

            @Html.ValidationMessageFor(x => x.senha, "", new { @class = "text-danger" })

        </div>
    </div>

    

    <input type="submit" class="button button-a button-big button-rouded" value="ENTRAR"><br />

    <br />
    @Html.ActionLink("Clique aqui para se cadastrar", "CadLogin", "Adm") | @Html.ActionLink("Voltar", "Index", "Home")

</fieldset>
}
